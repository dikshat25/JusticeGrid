class DocumentParserService:
    async def extract_text(self, document_url: str) -> str:
        # Placeholder for OCR or PDF text extraction
        return "Mocked extracted text from document."

parser_service = DocumentParserService()
